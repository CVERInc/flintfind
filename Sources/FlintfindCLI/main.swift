import FlintfindKit
import Foundation

// The terminal half, still a stub. It exists now so the package's shape is real — a library, a CLI
// that reads it, and tests — before 200 lines of engine arrive to fill it. Building the frame first
// is what stops a port from discovering its layout is wrong at the end.
print("flintfind — not yet. The engine is being ported; use `ff` (Python) meanwhile.")
_ = Scope.mine("/")
